#!/bin/bash
# Following is the hello world program written in bash script
# The first line is called "Shabang" which allows executing permisisions to the bash script

echo "Hello, world!"

# make sure to do chmod +x for granting the executable permissions if required