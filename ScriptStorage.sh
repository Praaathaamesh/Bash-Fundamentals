#!/bin/bash

# Learn more about script storage anf linux architecture at: https://www.geeksforgeeks.org/linux-unix/linux-file-hierarchy-structure/

# store the important scripts at /usr/local/bin
# sudo mv <script.sh> /usr/local/bin/script ; no need to add the extension---linux can handle that
# sudo chown root:root ; change/grant the ownership to root
# export PATH=/usr/local/bin:$PATH ; to export the main path at $PATH environment variable